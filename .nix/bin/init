#! /usr/bin/env bash
#BUF_VERSION="1.3.1"
#BUF_BIN="$CACHE_DIR/bin"
#BUF_DIR="$CACHE_DIR/buf"
#
#if [ ! -d "$CACHE_DIR/bin" ]; then
#  mkdir -p $CACHE_DIR/bin;
#
#  if [ ! -d "$BUF_DIR" ]; then
#    mkdir -p $BUF_DIR;
#    curl -sSL "https://github.com/bufbuild/buf/releases/download/v${BUF_VERSION}/buf-$(uname -s)-$(uname -m).tar.gz" | tar -xvzf - -C "${BUF_DIR}" --strip-components 1
#    ln -s $BUF_DIR/bin/* $CACHE_DIR/bin
#  fi
#fi
#
